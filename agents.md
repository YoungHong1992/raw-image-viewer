* 说中文
* 说人话
* 必须要画 ASCII 示意图
* 可以针对用户提示词到 '/Users/anni/.codex/commands' 找到专用提示词，简单的任务不需要创建专用提示词，如果找不到的话就在 commands 目录下创建一个专用提示词，并且在你开始输出之前告诉我你用了哪个 command，简单任务可以不用 command
* 你可以在 /Users/anni/.codex/code 目录下写代码
* 记忆必须写在 memory.md，要注意管理记忆，删除过时的记忆，不要把记忆文件弄得太长
* md里要用 mermaid 而不是 ASCII
* 不需要每次都生成代码，你要根据情况判断
* 如果要修改代码的话，你要先用 ASCII 表格输出测试用例，我回复OK后你再修改，这样我才知道你改完后会是什么样的