# 只保留扩展运行必需的文件
# 白名单策略：忽略所有文件，只明确包含必要的文件

# 忽略所有文件
**

# 包含必要的文件和目录
!package.json
!README.md
!LICENSE
!out/extension/**
!out/shared/**
!dist/webview/**

# 确保忽略源代码（因为已经编译到out目录）
!src/
src/**
!webview/
webview/**